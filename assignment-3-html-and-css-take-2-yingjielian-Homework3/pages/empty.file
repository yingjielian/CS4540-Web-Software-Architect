this is just a file...git will not a commit a directory unless it contains a file, so here's a file.  Once you add an html page in this directory, this file is no longer needed, but you don't have to remove it if you don't want to.  it's a goofiness within git...